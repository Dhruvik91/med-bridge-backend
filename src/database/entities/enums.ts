export enum UserRole {
  doctor = 'doctor',
  hospital = 'hospital',
  admin = 'admin',
}

export enum ApplicationStatus {
  pending = 'pending',
  reviewed = 'reviewed',
  shortlisted = 'shortlisted',
  rejected = 'rejected',
  hired = 'hired',
}

export enum JobStatus {
  active = 'active',
  closed = 'closed',
  draft = 'draft',
}
